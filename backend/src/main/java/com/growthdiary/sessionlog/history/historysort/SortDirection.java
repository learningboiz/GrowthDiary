package com.growthdiary.sessionlog.history.historysort;

/**
 * The {@code SortDirection} enum represents sorting directions, either in ascending (ASC) or descending (DESC) order.
 */
public enum SortDirection {

    ASC,

    DESC
}
